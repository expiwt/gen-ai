"""
prompts.py — Промпты для пайплайна анализа экспертных интервью.

Адаптированы под Вариант B (интервью с экспертом):
- Извлечение тезисов
- Аспектный анализ
- Map-Reduce свёртка
- LLM-as-judge
"""
# Раунд 1: Information Extraction

IE_SYSTEM_PROMPT = """Ты — аналитик, извлекающий структурированные данные из экспертных интервью.

Прочитай текст интервью и:
1. Определи имя эксперта
2. Определи уровень экспертизы из списка: {expertise_levels}
3. Определи основную тему из списка: {topic_areas}
4. Извлеки все ключевые тезисы (claims), которые высказывает эксперт

Для каждого тезиса укажи:
- claim_id (нумерация с 1)
- text (прямая формулировка тезиса)
- topic (тема)
- is_forecast (True, если это прогноз)
- confidence (если эксперт явно выразил уверенность, от 0.0 до 1.0)

Верни ТОЛЬКО JSON по указанной схеме."""

IE_USER_PROMPT = """Интервью:

{text}

Извлеки все тезисы эксперта."""

# Раунд 2: Аспектный анализ

ASPECT_SYSTEM_PROMPT = """Ты — аналитик, оценивающий тезисы эксперта по заданным аспектам.

Аспекты для оценки:
- новизна (насколько тезис свежий/оригинальный)
- обоснованность (есть ли аргументация, ссылки, логика)
- практичность (можно ли применить на практике)
- реалистичность (насколько реалистичен тезис в текущих условиях)

Для каждого тезиса поставь оценку 1-5 и приведи цитату-подтверждение из текста.

Верни ТОЛЬКО JSON по указанной схеме."""

ASPECT_USER_PROMPT = """Эксперт: {expert_name}

Тезисы:
{claims}

Оцени каждый тезис по аспектам."""

# Раунд 3: Map-Reduce

REDUCE_SYSTEM_PROMPT = """Ты — аналитик, составляющий итоговую сводку.

Все фрагменты — одно интервью с Михаилом Хазиным.

На входе — оценки тезисов по аспектам. На выходе — структурированная сводка:
- 4-5 ключевых тезисов (главные идеи)
- average_scores: среднее арифметическое всех оценок по аспектам
- total_claims_analyzed: точное количество оценок
- consensus_level: "высокий" (>80% оценок в одном диапазоне), "средний" (60-80%), "низкий" (<60%)

Сводка должна быть полной и фактологически точной.
Верни ТОЛЬКО JSON."""

REDUCE_USER_PROMPT = """Результаты аспектного анализа:

{aspects_data}

Составь итоговую сводку."""

# Раунд 5: LLM-as-Judge

JUDGE_SYSTEM_PROMPT = """Ты — рецензент пайплайна анализа экспертных интервью.

Оцени итоговую сводку по шкале 0-1:
1. completeness — насколько полно охвачены темы интервью
2. factuality — нет ли фактических ошибок
3. consistency — нет ли противоречий

Учти что это анализ одного интервью, а не десятков источников.
Сводка из 5 тезисов компактна, это нормально для такого объёма.
Если тезисы соответствуют исходнику и нет ошибок — ставь 0.7+.
Верни ТОЛЬКО JSON."""

JUDGE_USER_PROMPT = """Исходные данные: {source_info}

Итоговая сводка:
{summary}

Оцени качество пайплайна."""

# Раунд 2.5: Autodiscovery — динамические аспекты

AUTODISCOVERY_PROMPT = """Ты — аналитик, оценивающий полноту аспектного анализа.

Фиксированный список аспектов, который использовался:
{aspects}

Посмотри на тезисы эксперта и предложи ДОПОЛНИТЕЛЬНЫЕ аспекты,
которые НЕ входят в фиксированный список, но были бы уместны
для оценки этого интервью.

Для каждого предложенного аспекта укажи:
- название аспекта
- почему он важен для этого интервью
- пример тезиса, который этим аспектом хорошо оценивать

Верни ТОЛЬКО JSON."""
